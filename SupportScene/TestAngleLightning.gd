extends Node2D


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
#func _ready():
##	var xy = $Position2D.position - $WhiteTexture.position
#	var tes_dis = $WhiteTexture.position.distance_to($Position2D.position)
#	var tes_angle = $Position2D.position.angle_to_point($WhiteTexture.position)
#	print (tes_dis/512.0)
#	print (rad2deg(tes_angle))
#	var angle = tan(xy.y/xy.x)
#	print (xy.angle())
	


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
